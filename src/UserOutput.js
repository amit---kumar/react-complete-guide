import React from 'react';

const userOutput = (props) =>
{
    return(
        <div>
            <p>{props.name} this is first paragraph</p>
            <p>this is second paragraph</p>
        </div>
    );
} 

export  default userOutput;